{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "independent",
  "packages": ["packages/*"],
  "command": {
    "publish": {
      "registry": "https://npm.pkg.github.com/feedma"
    },
    "version": {
      "createRelease": "github",
      "conventionalCommits": true,
      "allowBranch": ["main", "develop"],
      "message": "chore(release): publish"
    }
  }
}
